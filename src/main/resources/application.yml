spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/distribusi_roko
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server.port: 8081

#  kafka:
#    bootstrap-servers: localhost:9092
